---
"kilo-code": patch
---

Automatically condense the conversation and retry (up to 3 times) when a provider rejects a request for exceeding the model's context window, instead of failing the request immediately.
