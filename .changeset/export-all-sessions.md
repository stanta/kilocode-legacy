---
"kilo-code": minor
---

Add `/export_all_sessions` slash command and `kilo-code.exportAllSessions` command to export all Kilo Code sessions, preserving the raw on-disk task files and writing the `task_history.json` index from global state.
